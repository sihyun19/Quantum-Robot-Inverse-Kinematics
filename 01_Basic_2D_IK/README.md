# Phase 1: 2D Planar Control

## 📝 개요 (Overview)
이 모듈은 **3개의 회전 관절(Revolute Joints)**을 가진 평면 로봇 팔을 제어합니다.
로봇 팔의 끝단(End-effector)이 목표 지점 `(2.1, 0.8)`에 도달하도록 각 관절의 각도($\theta_1, \theta_2, \theta_3$)를 양자 회로가 찾아냅니다.

## ⚙️ 로봇 모델 (Robot Model)
- **링크 길이:** $L_1 = L_2 = L_3 = 1.0$
- **순운동학 (Forward Kinematics):**
  $$x = L_1 \cos(\theta_1) + L_2 \cos(\theta_1 + \theta_2) + L_3 \cos(\theta_1 + \theta_2 + \theta_3)$$
  $$y = L_1 \sin(\theta_1) + L_2 \sin(\theta_1 + \theta_2) + L_3 \sin(\theta_1 + \theta_2 + \theta_3)$$

## ⚛️ 양자 회로 구조 (Quantum Circuit)
PennyLane의 `default.qubit` 시뮬레이터를 사용하며, 3개의 큐비트(Qubits)로 구성됩니다.

1.  **Embedding:** 학습 파라미터를 `RX`, `RY` 게이트의 회전각으로 인코딩합니다.
2.  **Entanglement:** `CNOT` 게이트를 사용하여 큐비트 간의 상호작용(얽힘)을 유도합니다. (순방향 & 역방향)
3.  **Measurement:** 각 큐비트의 `PauliZ` 기댓값을 측정하여 관절 각도로 매핑합니다.
    - 매핑 범위: $-\pi \sim +\pi$

## 📉 최적화 (Optimization)
- **Optimizer:** Adam Optimizer (`stepsize=0.1`)
- **Cost Function:** 목표 위치와 현재 위치 간의 유클리드 거리 제곱 (MSE).
- **Early Stopping:** 오차가 `0.0001` 이하가 되면 학습을 조기 종료합니다.

## 📊 결과 예시 (Result)
*아래 이미지는 학습 완료 후 시각화된 로봇의 자세입니다.*

![01_Basic_2D_IK](./result.png)
